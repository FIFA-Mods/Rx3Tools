<UserControl x:Class="Rx3Tools.FloatSpinBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Name="Root">
    <Border x:Name="SpinBoxBorder">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="{StaticResource CB_Background}"/>
                <Setter Property="BorderBrush" Value="{StaticResource CB_Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource CB_HoverAccent}"/>
                    </Trigger>
                    <Trigger Property="IsKeyboardFocusWithin" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource CB_HoverAccent}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="BorderBrush" Value="#FF3A3A3A"/>
                        <Setter Property="Background" Value="#FF1A1A1A"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TxtValue"
                     Grid.Column="0"
                     Text="{Binding Value, ElementName=Root, StringFormat=F5}"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="{StaticResource CB_Text}"
                     CaretBrush="{StaticResource CB_Text}"
                     SelectionBrush="{StaticResource CB_HoverAccent}"
                     VerticalContentAlignment="Center"
                     Padding="5,0"
                     PreviewTextInput="TxtValue_PreviewTextInput"
                     DataObject.Pasting="TxtValue_Pasting"
                     LostFocus="TxtValue_LostFocus"/>

            <Border Grid.Column="1"
                    BorderBrush="{StaticResource CB_Border}"
                    BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <RepeatButton Grid.Row="0"
                                  Content="▲"
                                  Click="UpButton_Click"
                                  Style="{StaticResource SpinnerRepeatButtonStyle}"/>
                    <Rectangle Grid.Row="1" Height="1" Fill="{StaticResource CB_Border}"/>
                    <RepeatButton Grid.Row="2"
                                  Content="▼"
                                  Click="DownButton_Click"
                                  Style="{StaticResource SpinnerRepeatButtonStyle}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
